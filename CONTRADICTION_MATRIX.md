# Contradiction Matrix

Audit date: 2026-08-27  
Audit baseline: `master` at `2eacec16192b1d2f33238180c11d805526367a5c`  
Scope: active manuscript, canonical corpus, evidence assignment CSV, source map, reference manifest, BibTeX, figures, scripts, and release metadata.

The canonical empirical record layer is `corpus/included/` plus `references/record-id-map.csv`. The source layer is tracked separately in `references/source-map.csv` and `references/manifest.csv`; a source-layer count is never used as a record-layer count.

| ID | Artifacts | Conflict | Status after audit | Resolution / required action |
|---|---|---|---|---|
| X01 | `refs.bib`, `references/source-map.csv`, `references/manifest.csv` | BibTeX had 50 cited keys, while the source map and manifest each had 49 usable key mappings; stale `cybergym2025` replaced current `cybergym2026`, and `darpa2023aixcc` was absent. | Resolved | Source map and manifest now contain all 50 BibTeX keys. The DARPA 2023 announcement is a source-layer background anchor, not an additional canonical empirical record. |
| X02 | `corpus/included/`, record map, assignment CSV, Tables II/IV | 36 canonical records versus 50 bibliography/source entries. | Scoped, not an error | The layers track different units. Canonical record counts remain 36; source-layer count is 50. |
| X03 | `r3survey2026.yaml`, assignment CSV, `main.tex` | R3 was marked `in-window` in canonical metadata but `OUT` in the manuscript; arXiv submission is 2026-07-01, after the 2026-06-30 window. | Resolved | Canonical metadata now says `out-of-window`; protocol schema now permits that value; manuscript retains R3 only as a comparator. |
| X04 | `refs.bib:r2slr2024`, local R2 PDF/TXT, manifest | BibTeX listed six unrelated authors; the local source and current arXiv record identify Enna Basic and Alberto Giaretta. | Resolved | BibTeX and canonical YAML aligned to the primary record. |
| X05 | `refs.bib:rissebohme-wrongexam2024`, local PDF | BibTeX omitted Jing Liu from the three-author primary record. | Resolved | Jing Liu restored. |
| X06 | `refs.bib:r7sokaixcc2026`, arXiv record/local PDF | BibTeX listed 23 names, including Brian J. Lee and Joshua Wang; arXiv v5 lists 21 authors and the local PDF matches that list. | Resolved | BibTeX aligned to the 21-author arXiv record. |
| X07 | `refs.bib:r3survey2026`, local R3 PDF, arXiv | BibTeX used `cs.CR`; the arXiv record and PDF identify `cs.SE`. | Resolved | Primary class corrected to `cs.SE`. |
| X08 | `refs.bib` organizational authors | Group authors were not consistently protected, and personal affiliations were embedded in some author names. | Resolved for active malformed cases | Group names are braced; personal names and affiliations are separated in active entries. |
| X09 | R4 BibTeX/YAML/manifest/assignment/Table II | R4 was recorded as 2025, but DOI/index metadata identifies ACM CSUR volume 58(5), article 134, published in 2026. | Resolved | R4 metadata normalized to 2026 and the in-window status is retained. Full text remains paywalled. |
| X10 | `references/manifest.csv` NVD row | The row pointed to `nvd-cve-2025-6965-NOTFETCHED.txt` and had shifted empty DOI/URL columns, despite `nvd-cve-2025-6965-full.txt` being present. | Resolved | URL, local text path, field alignment, and provenance were repaired. |
| X11 | AIxCC prose, ToB report, R7 SoK | ToB reports 48 challenges across 23 repositories; R7 reports 48 scored projects from 24 OSS-Fuzz repositories. | Open but explicit | Both values remain, with source provenance and units named. They must not be merged into one denominator. |
| X12 | AIxCC scoring prose, Team Atlanta report | Team Atlanta reports `44.4% -> 0.9044` while the displayed complement formula independently evaluates to `0.9047`. | Resolved with qualified reporting | The manuscript preserves `0.9044` only as source-reported and records the independent recomputation as `0.9047`. |
| X13 | DARPA closeout, main prose | Earlier prose treated 63 as “63 synthetic vulnerabilities”; the official closeout says 54 unique synthetic vulnerabilities across 63 final challenges. | Resolved | Manuscript now distinguishes the 54-vulnerability numerator from the 63-challenge denominator. |
| X14 | GTG limitation prose, taxonomy table | The manuscript said GTG was “unresolved” but also “resolved” by the weakest-missing-condition rule; the table used `unresolved` while canonical schema uses `uncertain`. | Resolved | Uncertainty is preserved; `uncertain` is the canonical field value; the narrative says the rule preserved rather than resolved uncertainty. |
| X15 | A3 sensitivity narrative/table | Previous sensitivity table forced every scenario to zero while retaining blockers whose conditions were supposedly relaxed; the no-human corollary duplicated the reporting blocker. | Resolved | Predicate variables `P,X,V,R,E` and explicit counterfactual qualifier counts replace the forced-zero logic. |
| X16 | `figures/autonomy-loop-map.tex`, Table II taxonomy | Figure comment said context = 20, while the taxonomy separates 2 loop-transition records and 18 off-axis/context records. | Resolved | Figure comment now names 2 transition records and 18 context records. |
| X17 | `figures/timeline-f3.tex` | Timeline used `2024-10..11`, `2025-05..06`, etc., which visually suggested continuous intervals for discrete events. | Resolved | Labels now use slash-separated discrete months and an explanatory comment. |
| X18 | protocol, canonical unit fields | Several YAML/CSV records used non-schema values such as `system(experiment-benchmark)`, `system(pipeline)`, and `system(participant-account)`. | Resolved | These were normalized to `system`; experiment/system semantics remain in the separate fields and notes. |
| X19 | `main.tex`, corpus generator claim | Manuscript claimed counts were “computed programmatically,” but no active table-generation script was found; the table body was manually frozen. | Resolved | Wording now says counts are reconciled against YAML/CSV and calls the table a frozen audit view. |
| X20 | canonical YAMLs, protocol CP4 rule | Many canonical records retained `[PENDING-CLOSEREAD]`, `(verify)`, `TBD`, or `[TO-VERIFY]` despite local primary PDFs being present. | Resolved for locally evidenced records; residual external gap remains | Verified local fields were cleaned. R4/R6 remain explicitly metadata-only/paywalled; the paywalled note preserves the unresolved access limitation. |
| X21 | `scripts/verify_final.py`, `RELEASE_MANIFEST.md` | Verification expected 17 pages and historical hashes; current baseline PDF was 18 pages and the release manifest referenced an older commit. | Resolved | Verification now expects the audited 18-page build; release metadata records the rebuilt hashes and baseline commit. |
| X22 | `scripts/scripts_audit_final.py`, active PDF | Script inspected `sok-coevolution.pdf` and only recognized simple `\cite{}` forms; the active deliverable is `main.pdf`. | Resolved | The script now targets `main.pdf`, recognizes citation variants, checks source-layer key parity, and validates canonical corpus fields. |

## Contradictions deliberately retained

The 23-versus-24 repository discrepancy (X11) is not silently repaired because the two source accounts use different denominators. The Team Atlanta 0.9044 value (X12) is retained only with its source-reported qualifier. GTG-1002 remains uncertain because the incident report does not provide enough evidence for a definitive autonomy level. R4 and R6 remain metadata-only where their full text is unavailable; no load-bearing full-text claims are assigned to them.
